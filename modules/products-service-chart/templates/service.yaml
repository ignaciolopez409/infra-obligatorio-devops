apiVersion: v1
kind: Service
metadata:
  name: {{ include "products-service-chart.fullname" . }}
  labels:
    {{- include "products-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "products-service-chart.selectorLabels" . | nindent 4 }}
