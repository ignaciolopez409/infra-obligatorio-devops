apiVersion: v1
kind: Service
metadata:
  name: {{ include "payments-service-chart.fullname" . }}
  labels:
    {{- include "payments-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payments-service-chart.selectorLabels" . | nindent 4 }}
