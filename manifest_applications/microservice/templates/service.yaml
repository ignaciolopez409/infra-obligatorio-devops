apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservice.fullname" . }}
  labels:
    app: {{ include "microservice.nameOverride" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "microservice.nameOverride" . }}
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080